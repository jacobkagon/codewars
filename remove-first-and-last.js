function removeChar(str){
    //You got this!
    //use substring and slice to remove the first and last elements of a string;
     const newStr = str.substring(1).slice(0, -1)
     
     return newStr
   };